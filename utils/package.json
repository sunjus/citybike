{
  "name": "utils",
  "version": "1.0.0",
  "description": "* scripts to read csv and import to the database * sqlite3 for DB",
  "main": "read_csv.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv": "^6.2.0"
  }
}
